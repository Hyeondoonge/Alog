export interface User {
  userId: string | null;
  profile_fileName: string | null;
  api_accessToken: string | null;
  api_refreshToken: string | null;
  accessToken: string | null;
  refreshToken: string | null;
}
